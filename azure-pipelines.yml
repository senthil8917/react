trigger:
- main

variables:
- group: Sandbox-Config

stages:
- stage: Sandbox
  displayName: "Deploy to Sandbox"
  jobs:
  - job: DeployToSandbox
    steps:
    - script: echo "Environment:"
    - script: echo "Using API"
    - script: echo "Deploying to Sandbox..."
